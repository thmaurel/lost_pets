<h1><%= @pet.name %></h1>
<p>Species: <%= @pet.species %></p>
<p>found on <%= @pet.found_on %> in <%= @pet.address %></p>


<%= link_to 'Edit your pet', edit_pet_path(@pet), class: 'btn btn-primary' %>
<%= link_to 'Delete this pet', pet_path(@pet), data: {turbo_method: 'delete'}, class: 'btn btn-primary' %>
<%= link_to 'Back', pets_path %>
